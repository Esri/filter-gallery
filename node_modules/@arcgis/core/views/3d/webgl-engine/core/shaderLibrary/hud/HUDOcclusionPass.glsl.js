/*
All material copyright ESRI, All Rights Reserved, unless otherwise specified.
See https://js.arcgis.com/4.23/esri/copyright.txt for details.
*/
import{ReadLinearDepth as e}from"../output/ReadLinearDepth.glsl.js";import{multipassGeometryTest as t}from"../shading/MultipassGeometryTest.glsl.js";import{RgbaFloatEncoding as r}from"../util/RgbaFloatEncoding.glsl.js";import{glsl as o}from"../../shaderModules/interfaces.js";function a(a,i){i.multipassGeometryEnabled&&a.vertex.include(t),i.multipassTerrainEnabled&&a.varyings.add("depth","float"),a.vertex.code.add(o`
  void main(void) {
    vec4 posProjCenter;
    if (dot(position, position) > 0.0) {
      // Render single point to center of the pixel to avoid subpixel
      // filtering to affect the marker color
      ProjectHUDAux projectAux;
      vec4 posProj = projectPositionHUD(projectAux);
      posProjCenter = alignToPixelCenter(posProj, viewport.zw);

      ${i.multipassGeometryEnabled?o`
        // Don't draw vertices behind geometry
        if(geometryDepthTest(.5 + .5 * posProjCenter.xy / posProjCenter.w, projectAux.posView.z)){
          posProjCenter = vec4(1e038, 1e038, 1e038, 1.0);
        }`:""}

      ${i.multipassTerrainEnabled?"depth = projectAux.posView.z;":""}
      vec3 vpos = projectAux.posModel;
      if (rejectBySlice(vpos)) {
        // Project out of clip space
        posProjCenter = vec4(1e038, 1e038, 1e038, 1.0);
      }

    } else {
      // Project out of clip space
      posProjCenter = vec4(1e038, 1e038, 1e038, 1.0);
    }

    gl_Position = posProjCenter;
    gl_PointSize = 1.0;
  }
  `),i.multipassTerrainEnabled&&a.fragment.include(e),a.fragment.uniforms.add("terrainDepthTexture","sampler2D"),a.fragment.uniforms.add("nearFar","vec2"),a.fragment.uniforms.add("inverseViewport","vec2"),a.fragment.include(r),a.fragment.code.add(o`
  void main() {
    gl_FragColor = vec4(1, 1, 1, 1);
    ${i.multipassTerrainEnabled?o`

          vec2 uv = gl_FragCoord.xy * inverseViewport;

          //Read the rgba data from the texture linear depth
          vec4 terrainDepthData = texture2D(terrainDepthTexture, uv);

          float terrainDepth = linearDepthFromFloat(rgba2float(terrainDepthData), nearFar);

          //If HUD vertex is behind terrain and the terrain depth is not the initialize value (e.g. we are not looking at the sky)
          //Mark the HUD vertex as occluded by transparent terrain
          if(depth < terrainDepth && terrainDepthData != vec4(0,0,0,1)){
            gl_FragColor.g = 0.5;
          }`:""}
  }
  `)}export{a as HUDOcclusionPass};
