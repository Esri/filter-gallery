/*
All material copyright ESRI, All Rights Reserved, unless otherwise specified.
See https://js.arcgis.com/4.23/esri/copyright.txt for details.
*/
import{ReadLinearDepth as e}from"../views/3d/webgl-engine/core/shaderLibrary/output/ReadLinearDepth.glsl.js";import{multipassTerrainTest as r}from"../views/3d/webgl-engine/core/shaderLibrary/shading/MultipassTerrainTest.glsl.js";import{ColorConversion as a}from"../views/3d/webgl-engine/core/shaderLibrary/util/ColorConversion.glsl.js";import{glsl as i}from"../views/3d/webgl-engine/core/shaderModules/interfaces.js";import{ShaderBuilder as d}from"../views/3d/webgl-engine/core/shaderModules/ShaderBuilder.js";import{VertexAttribute as o}from"../views/3d/webgl-engine/lib/VertexAttribute.js";function t(t){const s=new d;return s.extensions.add("GL_OES_standard_derivatives"),s.attributes.add(o.POSITION,"vec3"),s.attributes.add(o.UV0,"vec2"),s.vertex.uniforms.add("proj","mat4").add("view","mat4"),s.varyings.add("vUV","vec2"),t.multipassTerrainEnabled&&s.varyings.add("depth","float"),s.vertex.code.add(i`
    void main(void) {
      vUV = uv0;
      ${t.multipassTerrainEnabled?"depth = (view * vec4(position, 1.0)).z;":""}
      gl_Position = proj * view * vec4(position, 1.0);
    }
  `),t.multipassTerrainEnabled&&(s.fragment.include(e),s.include(r,t)),s.fragment.uniforms.add("size","vec2").add("color1","vec4").add("color2","vec4"),s.fragment.include(a),s.fragment.code.add(i`
    void main() {
      ${t.multipassTerrainEnabled?"terrainDepthTest(gl_FragCoord, depth);":""}
      vec2 uvScaled = vUV / (2.0 * size);

      vec2 uv = fract(uvScaled - 0.25);
      vec2 ab = clamp((abs(uv - 0.5) - 0.25) / fwidth(uvScaled), -0.5, 0.5);
      float fade = smoothstep(0.25, 0.5, max(fwidth(uvScaled.x), fwidth(uvScaled.y)));
      float t = mix(abs(ab.x + ab.y), 0.5, fade);

      gl_FragColor = mix(color2, color1, t);
      ${t.oitEnabled?"gl_FragColor = premultiplyAlpha(gl_FragColor);":""}
    }
  `),s}const s=Object.freeze({__proto__:null,build:t});export{s as C,t as b};
